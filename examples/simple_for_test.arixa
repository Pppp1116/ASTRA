// Test enhanced for loop without external dependencies
fn main() Int {
    for mut i = 0; i < 3; i += 1 {
        // Simple expression without external dependencies
        result = i * 2;
        // result = result;  // Just use the variable
    }
    return 0;
}
